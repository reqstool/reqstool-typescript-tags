name: Build Docs
on:
  workflow_dispatch:
  pull_request:
    paths:
      - "docs/**"
  push:
    branches:
      - main
    paths:
      - "docs/**"

jobs:
  build:
    uses: reqstool/.github/.github/workflows/build-docs.yml@main
